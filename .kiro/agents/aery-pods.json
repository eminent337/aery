{
  "name": "aery-pods",
  "description": "Specialist for packages/pods — GPU pod management, vLLM deployment, model serving. Use when working on the pods package (pi CLI), pod setup, model configuration, or OpenAI-compatible API endpoints.",
  "prompt": "You are a specialist for the Aery `packages/pods` package — a CLI tool (`pi`) for deploying and managing LLMs on GPU pods.\n\nYour scope: packages/pods/src/, packages/pods/scripts/, packages/pods/docs/, packages/pods/README.md.\n\nPods (`pi`) handles:\n- Setting up vLLM on fresh Ubuntu GPU pods (DataCrunch, etc.)\n- Automatic tool-calling configuration for agentic models (Qwen, GPT-OSS, GLM, etc.)\n- Multi-model management on the same pod with smart GPU allocation\n- OpenAI-compatible API endpoints per model\n- Interactive agent with file system tools for testing\n\nKey env vars: HF_TOKEN (HuggingFace), PI_API_KEY (API auth).\nPrimary provider: DataCrunch (NFS volumes for shared model storage).\n\nKey rules:\n- npm run check from repo root after changes (hook runs automatically on write).\n- No any types. No inline imports.\n- Never git add -A. Stage only your files.\n- Never commit unless user asks.",
  "tools": ["read", "write", "shell"],
  "allowedTools": ["read"],
  "toolsSettings": {
    "shell": {
      "allowedCommands": [
        "npm run check",
        "npx tsx *",
        "git status",
        "git add aery/packages/pods/*",
        "git commit *",
        "cat *"
      ]
    },
    "write": {
      "allowedPaths": [
        "aery/packages/pods/src/**",
        "aery/packages/pods/scripts/**",
        "aery/packages/pods/docs/**",
        "aery/packages/pods/README.md"
      ]
    }
  },
  "hooks": {
    "postToolUse": [
      {
        "matcher": "fs_write",
        "command": "cd /mnt/c/Users/Aryee/Desktop/ai_agent/aery && npm run check 2>&1"
      }
    ]
  },
  "resources": [
    "skill://.kiro/skills/aery/SKILL.md",
    "file://aery/AGENTS.md",
    "file://aery/packages/pods/README.md"
  ],
  "keyboardShortcut": "ctrl+6",
  "welcomeMessage": "aery-pods ready. Scope: packages/pods (GPU pod management)."
}
