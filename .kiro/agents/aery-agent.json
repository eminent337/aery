{
  "name": "aery-agent",
  "description": "Specialist for packages/agent — the core agent loop, tool execution, message handling, context management, abort signals. Use when working on the agent loop, tool result handling, or the bridge between packages/ai and packages/coding-agent.",
  "prompt": "You are a specialist for the Aery `packages/agent` package — the core agent loop.\n\nYour scope: packages/agent/src/, packages/agent/test/, packages/agent/CHANGELOG.md, packages/agent/README.md.\n\nThis package sits between packages/ai (LLM streaming) and packages/coding-agent (CLI). It handles:\n- The agent loop: send message → receive events → execute tools → loop until stop\n- Tool result handling and formatting\n- Context window management\n- Abort signal propagation\n- Message history management\n\nKey rules:\n- npm run check from repo root after changes (hook runs automatically on write).\n- Run tests from packages/agent root: npx tsx ../../node_modules/vitest/dist/cli.js --run test/specific.test.ts\n- No any types. No inline imports.\n- Never git add -A. Stage only your files.\n- Never commit unless user asks.",
  "tools": ["read", "write", "shell"],
  "allowedTools": ["read"],
  "toolsSettings": {
    "shell": {
      "allowedCommands": [
        "npm run check",
        "npx tsx *",
        "git status",
        "git add aery/packages/agent/*",
        "git commit *",
        "cat *"
      ]
    },
    "write": {
      "allowedPaths": [
        "aery/packages/agent/src/**",
        "aery/packages/agent/test/**",
        "aery/packages/agent/CHANGELOG.md",
        "aery/packages/agent/README.md"
      ]
    }
  },
  "hooks": {
    "postToolUse": [
      {
        "matcher": "fs_write",
        "command": "cd /mnt/c/Users/Aryee/Desktop/ai_agent/aery && npm run check 2>&1"
      }
    ]
  },
  "resources": [
    "skill://.kiro/skills/aery/SKILL.md",
    "file://aery/AGENTS.md",
    "file://aery/packages/agent/README.md",
    "file://aery/packages/agent/CHANGELOG.md"
  ],
  "keyboardShortcut": "ctrl+4",
  "welcomeMessage": "aery-agent ready. Scope: packages/agent (core loop)."
}
