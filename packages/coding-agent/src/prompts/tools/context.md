Report the current session's context usage: the total context window, the estimated tokens currently used, and the percent of the window consumed.

Call this when you want to check how much context you have left, before deciding whether to compact, drop heavy content, or trim. No arguments.
